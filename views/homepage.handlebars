<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        <ol class="post-list">
            {{#each post as |post|}}
            <li>
                {{> post-info post }}
            </li>
            {{/each}}
        </ol>
    </li>
    {{/each}}
</ol>>